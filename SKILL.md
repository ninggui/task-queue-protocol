---
name: user-task-queue-protocol
description: 用户待办队列协议：高峰时段任务先入队闲时再跑。触发：收到任何请求先判断时段。
slug: task-queue-protocol
displayName: 待办队列协议
version: 1.0.0
---

# 用户待办队列协议（Task Queue Protocol）

## 背景（2026-08-18 用户纠正固化）

用户明确要求：**除明说"立刻/现在/紧急"外，所有请求一律入队，非高峰统一处理**。本次会话 11:44 高峰时段直接执行了任务，被用户纠正："现在是高峰时段，我给你发的任务你都记住，在闲时的时候再运行？为什么又忘记了？"

## 核心规则

1. **判断先行**：收到任何请求，先 `date '+%Y-%m-%d %H:%M:%S %A'` 看当前时间
   - **高峰**（北京 9-12 / 14-18）→ 只入队，不执行
   - **非高峰**（12-14 / 18-次日9）→ **立即执行**，不等任何固定批次（2026-08-21 用户纠正：当前时刻已在非高峰窗口内就直接做，不要排到 03:00/12:10/18:10 等固定点）
2. **入队仅限高峰到达**：只有高峰时段收到的请求才入队，回复"已入队，闲时执行"，给预计窗口（下次 12:10 或 18:10）
3. **紧急信号**：用户说"现在/立刻/马上/紧急"才直接执行（非高峰时即使没说也算可执行）
4. **误执行补救**：若高峰误执行了，在队列条目标注"高峰期误执行"并承认，不掩饰
5. **禁止锚定固定点**：批量处理 cron（12:10/18:10）和夜间自主学习（03:00）是**高峰入队任务的兜底**，不是非高峰到达请求的延迟器；非高峰到达一律即时做

## 闲时操作节约（2026-08-21 用户纠正固化）

- **任何批量/耗时/重操作（验证、试用、批量处理、长任务）默认放闲时低价时段（北京 12-14 / 18-次日9）执行**，不在高峰对话中立即跑
- 原因：DeepSeek 峰谷价差约 2 倍——高峰跑批量任务 = 双倍 API 费用；闲时执行=成本减半
- 推荐任务/给方案时，若涉及执行动作，必须标注建议执行时段；高峰到达的批量任务入队，闲时再跑
- 用户说"以上先不做"不代表遗忘——规则不变，等闲时窗口

`/home/user/queue/pending_tasks.md`：
```markdown
## 待处理
- [ ] 任务 | 入队时间 | 优先级(P0/P1/P2) | 备注
## 已完成
- [x] 任务 | 入队时间 | 优先级 | ✅ 完成说明
```

- 每完成一条 → 从"待处理"移到"已完成"并标注 ✅
- 批量处理 cron：`待办队列批量处理` 每天 12:10/18:10 自动跑队列

## 相关机制

- 规则审计（user-rules-audit）：每晚检查用户近期规则是否都在 memory/skill 中——队列协议本身也是被审计的规则之一
- 记忆：memory 已有队列规则一句话；本 skill 是详细版
